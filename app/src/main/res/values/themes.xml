<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- A bare NoActionBar theme; all real styling is done by Compose (SukoonTheme). -->
    <style name="Theme.Sukoon" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/sukoon_bg</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
